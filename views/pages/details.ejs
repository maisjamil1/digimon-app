<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <style>
        div{
    
    display: grid;
    grid-template-columns: repeat(auto-fill,minmax(300px,1fr));
    grid-gap:50px;
}
section{
    margin: 0 auto;
    padding:10px;
    border:5px solid black;
    text-align: center;
    
    
}

    </style>
    <h1>details page</h1>
<nav>
    <a href="/"><li>home</li></a>
    <a href="/renderFAV"><li>favirouts</li></a>
    <a href="/renderSearch"><li>search page</li></a>
    
</nav>
    <section>
        <h2><%=val.name%></h2>
        <img src="<%=val.img%>" alt="">
        <p><%=val.level%></p>
        <form action="/update/<%=val.id%>?_method=PUT" method="POST">
            <input type="text" name="name" value="<%=val.name%>">
            <input type="text" name="img" value="<%=val.img%>">
            <input type="text" name="level" value="<%=val.level%>">
            <button>update</button>
        </form>


        <form action="/delete/<%=val.id%>?_method=delete" method="POST">
            <button>delete</button>
        </form>

    </section>
</body>

</html>